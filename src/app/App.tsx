/**
 * Application root shell.
 *
 * Intentionally minimal — providers, router, and layout are wired in later
 * implementation steps. Kept here so the app bootstraps and builds cleanly.
 */
export default function App() {
  return null
}
