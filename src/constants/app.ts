/**
 * Static application metadata.
 * Single source of truth for app name / description used across the shell.
 */
export const APP_NAME = 'MindShift' as const

export const APP_DESCRIPTION =
  'Train your mind to think clearly. Recognize and overcome cognitive biases through interactive decision-making.' as const

export const APP_LOCALE = 'en' as const
