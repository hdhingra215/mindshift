/**
 * Feedback surfaces.
 *
 * The controls a player uses to shape the soundscape and the haptics. The
 * systems themselves live in `@/lib/audio`, `@/lib/haptics` and
 * `@/lib/feedback`; these are the only components that read them as state.
 */

export { FeedbackSettings } from './feedback-settings'
export { SoundToggle } from './sound-toggle'
